package decoder

import (
	"github.com/happyhippyhippo/slate/config"
)

const (
	// ID is the base application container registration string
	// for all configuration decoder services.
	ID = config.ID + ".decoder"
)
